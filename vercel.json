{
  "buildCommand": "if [ -d ui ]; then cd ui; fi && npm run build",
  "outputDirectory": ".next",
  "installCommand": "if [ -d ui ]; then npm install && cd ui; fi && npm install",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}
