{
  "buildCommand": "cd ui && npm run build",
  "outputDirectory": "ui/.next",
  "installCommand": "npm install && cd ui && npm install",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}
