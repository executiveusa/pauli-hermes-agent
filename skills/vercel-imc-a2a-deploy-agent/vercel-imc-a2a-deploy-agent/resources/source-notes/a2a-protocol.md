# A2A Protocol Notes

Design decisions imported into this workspace:

- Publish an agent card under `.well-known`.
- Describe skills as discoverable capability units.
- Accept task requests over an authenticated HTTP interface.
- Keep internal memory and tool logic opaque to calling agents.
- Persist task state as explicit artifacts.
