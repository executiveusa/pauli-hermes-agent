# ICM Methodology Notes

Design decisions imported into this workspace:

- Folder structure is the orchestration layer.
- Stage contracts live in markdown.
- Outputs are handoff artifacts.
- Scripts handle deterministic mechanical work.
- Human/agent review gates exist between stages.
- Source-level improvements should happen in reusable stage/skill files rather than one-off outputs.
