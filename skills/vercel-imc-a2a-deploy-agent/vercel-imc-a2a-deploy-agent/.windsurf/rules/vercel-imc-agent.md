# Windsurf Rule: Vercel IMC Agent

Use `AGENTS.md` and `CONTEXT.md` as the root instructions. Never reveal secrets. Treat source-code edits in target apps as out of scope unless a later mission explicitly permits them.
