# Proof-First Revenue Loop — Pipeline

Flow: `01_scan -> 02_qualify -> 03_model -> 04_prove -> 05_judge -> 06_approve -> 07_test -> 08_verify -> 09_close -> 10_scale -> 11_compound`.

Each numbered folder has one job and its own `CONTEXT.md`. Each stage reads the prior stage output plus only named references. A stage is complete only when its output artifact exists and its human check, if required, is satisfied.

Stable factory references: `../../instructions/PROOF_FIRST_REVENUE_LOOP.md`, `../../instructions/HERMES.md`, `../../context/TASK_PROFILES.md`, `../../context/ENVELOPES.md`.

Product state belongs in the active mission/opportunity record and `../../memory/`, not in this factory folder.
