# PARÉ release state — 2026-09-04

## Identity
This state belongs to the owner's personal Hermes. MACS Digital Media / Agent Max remains a separate business-agent identity and authority boundary.

## Main
- Repo: `executiveusa/PARE`
- PR #5 merged revision: `c0e65919a54e2a7331d33ce256a2ea2960be6b6b`

## Active corrective slice
- PR: `executiveusa/PARE#6`
- Branch: `fix/pare-crossword-hero-postmerge-20260904`
- Latest observed head while writing this receipt: `2366d3f38ba8423ea861375bbdc3dfca16efcb5d`
- Netlify runtime-code preview payload: `234f4b8d5bfe1d94834a3dfacf3370a61b37f018`
- PR head differs after that payload by test/evidence-only work; re-resolve before any merge.

## Human-entry contract
Every hard human entry to Studio must first pass through the PARÉ effect landing:
- public root;
- reload;
- fresh tab/window;
- bookmark/direct URL;
- deep project link.

The landing grants one Studio boot and the Studio consumes the grant immediately. Internal SPA navigation after admission is not interrupted. Machine API/MCP/CLI callers are exempt from the visual doorway.

## PARÉ effect contract
The first public experience is a real static crossword, not a generated random glyph matrix. `PARÉ` is the target row; supporting words conceptually resolve around PROJECT / AGENTS / REDUCE / ÉPURE and are pared away as the visitor scrolls. The reveal precedes Studio entry.

## Studio Fusion / diffusion contract
`DiffusionOverlay` is mounted in the actual Studio shell. It follows the newest active assistant streaming prose block, supports prose/headings/lists/quotes, excludes code/tool surfaces, and never changes the semantic source text. It re-arms on new deltas after a provider pause so the effect does not disappear or double-render mid-stream. User toggle and reduced-motion behavior remain authoritative.

## Release proof still required
Before declaring production complete:
1. resolve exact PR head;
2. CI/focused checks green or explicitly disposition infrastructure-only blockage;
3. exact Netlify preview ready for runtime payload;
4. independent review clean;
5. merge only under owner release authority;
6. production Netlify exact merged payload ready;
7. root visibly enters PARÉ effect;
8. hard Studio deep link returns through effect;
9. real provider stream visibly diffuses and final semantic text is exact;
10. continue VPS/API/MCP/provider Gauntlet from PARÉ run state.

Evidence outranks claims.