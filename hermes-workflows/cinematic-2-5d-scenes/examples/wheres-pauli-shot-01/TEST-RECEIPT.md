# Where's Pauli — Shot 01 2.5D test receipt

## DECISION
`PROTOTYPE PASS / MOBILE HOLD`

The isolated prototype proves that a single real still can support the intended cinematic grammar without video or WebGL. Production promotion is not authorized; an actual 390×844 browser Gauntlet remains required.

## SOURCE + RIGHTS
- Source type: real satellite image.
- Image: `https://eoimages.gsfc.nasa.gov/images/imagerecords/71000/71608/Washington.A2004205.1930.250m.jpg`
- Source page: `https://visibleearth.nasa.gov/images/71608/phytoplankton-bloom-off-the-pacific-northwest/71609l`
- Credit used in prototype: `NASA Earth Observatory / NASA GSFC`.
- Final production use must retain traceable provenance and confirm reproduction/credit requirements.

## BUILD PROOF
Isolated Lovable visual lab:
- project id: `bff6a04a-827c-4de0-9e56-683ab0ee495c`
- preview: `https://id-preview--bff6a04a-827c-4de0-9e56-683ab0ee495c.lovable.app`
- commit: `f0aa8fb825843cd0c2ca047c72c2801746de0360`
- published: `false`
- backend/database/Vercel: none

Implementation uses:
- one NASA photographic source;
- three duplicated/masked CSS depth plates;
- scroll-driven differential `translate3d` + scale;
- 2D Canvas fog/cloud drift;
- CSS grain, vignette and scanlines;
- semantic HTML surveillance HUD;
- four scroll phases: hold → push → sweep → lock;
- `prefers-reduced-motion` handling;
- debug overlay default OFF.

## COST RECEIPT
Fal text-to-image was attempted once with a locked Shot 01 prompt. Fal returned `403 User is locked — Exhausted balance`. No image was generated. No retry or alternate paid-provider call was made. The workflow correctly fell back to the rights-cleared-real-image route.

Lovable reported `1.2` credits for building the isolated prototype.

## CANON CHECK
PASS at implementation/spec level:
- no Pauli visual;
- no Seattle identification in Shot 01 HUD;
- narrative remains regional search/acquisition;
- HUD is semantic HTML, not baked into the source image.

## VISUAL OBSERVATION
The generated Lovable project screenshot shows a restrained darkened Pacific Northwest satellite plate, small editorial labels, centered `SIGNAL ACQUIRED` state, vignette/scan texture, and no large dashboard/cyberpunk UI. This is directionally consistent with the requested mystery-thriller grammar.

## MOBILE STATUS
`HOLD — not browser-verified.`

Code includes mobile-oriented constraints and `overflow-x: hidden`, but the requested independent 390×844 Playwright pass could not be invoked because the current Lovable connector lacks `projects:write` scope for `send_message`. Do not claim flawless mobile behavior from code inspection alone.

Required next proof:
1. 390×844 screenshots at approximately 0%, 30%, 65%, 90%, 100% scroll;
2. horizontal overflow check;
3. reduced-motion screenshot;
4. runtime/console error check;
5. confirm source image loading and center-safe crop.

## GAUNTLET
Current result: `WIN at concept/prototype level; MOBILE HOLD before production.`

Single biggest unresolved gap: actual narrow-viewport evidence.

Smallest repair/test: reconnect Lovable with current permissions or run the same preview through an independent browser/Playwright worker at 390×844 without changing code.

## ROLLBACK
The lab is isolated and unpublished. Delete/ignore the Lovable project to roll back; no production system depends on it.

## NEXT
Use this exact skill/workflow for Shots 02–12 only after Shot 01 passes the real mobile Gauntlet. Each shot receives its own source/provenance and scene-spec; do not batch imagery before the visual grammar is proven.
